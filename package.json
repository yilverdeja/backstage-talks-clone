{
	"name": "backstage-talks-clone",
	"source": "index.html",
	"scripts": {
		"start": "parcel",
		"build": "npm run clean && parcel build",
		"clean": "rm -rf dist/*"
	},
	"devDependencies": {
		"parcel": "^2.12.0"
	}
}
